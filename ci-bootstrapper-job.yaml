apiVersion: batch/v1
kind: Job
metadata:
  name: auditorbootstrap-job
spec:
  template:
    spec:
      imagePullSecrets:
        - name: tylerorg
      containers:
      - name: auditorbootstrap-job
        image: "${repository}/${bootstrap}:${tag}"
        env:
        - name: CONSULCONTAINER
          value: consul:8500
        - name: DEPLOYMENTSTAGE
          value: CI
      restartPolicy: Never
