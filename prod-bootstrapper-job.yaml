apiVersion: batch/v1
kind: Job
spec:
  template:
    metadata:
      name: ${bootstrap}-job
    spec:
      containers:
      - name: ${bootstrap}-job
        image: "${repository}/${bootstrap}:${tag}"
        env:
        - name: CONSULCONTAINER
          value: consul:8500
        - name: DEPLOYMENTSTAGE
          value: PROD
      restartPolicy: Never
      imagePullSecrets:
        - name: tylerorg
