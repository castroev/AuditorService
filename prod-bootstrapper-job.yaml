apiVersion: batch/v1
kind: Job
metadata:
  name: auditorbootstrap
spec:
  template:
    spec:
      containers:
      - name: ${bootstrap}-job
        image: "${repository}/${bootstrap}:${tag}"
        env:
        - name: CONSULCONTAINER
          value: consul:8500
        - name: DEPLOYMENTSTAGE
          value: PROD
      restartPolicy: Never
      imagePullSecrets:
        - name: tylerorg
